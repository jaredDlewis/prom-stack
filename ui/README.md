# The Metric Stack

A literal stack of container metrics to model the following question:

**"Since a Docker container can have Docker installed inside of it, what does a system look like when you spin up a Docker container with instructions to recursively clone itself into another Docker container nested within itself?"**
